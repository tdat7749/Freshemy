import authorBookController from "./authorBook.controllers";
import authorController from "./author.controllers";
import bookController from "./book.controllers";
const controllers = {
    authorBookController: authorBookController,
    authorController: authorController,
    bookController: bookController
}
export default controllers;